# Screenshots

Images in this folder are captured with demo data only (`example.local` URLs). They do not reflect a real homelab deployment.

Regenerate after UI changes:

```bash
npm run build
npm run screenshots
```

The capture script uses a temporary SQLite database and never reads `data/unihomelabdash.sqlite`.
